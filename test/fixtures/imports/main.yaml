id: https://example.org/main
name: main
prefixes:
  linkml: https://w3id.org/linkml/
imports:
  - linkml:types
  - common/shared   # relative path, resolved against this file's directory
tree_root: Employee
classes:
  Employee:
    tree_root: true
    is_a: Party
    attributes:
      title:
        range: string
